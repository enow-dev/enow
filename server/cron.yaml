cron:
- description: event fetch
  url: /api/cron/fetch_events
  schedule: every 24 hours
  timezone: Asia/Tokyo

- description: read fix
  url: /api/cron/read_fix
  schedule: every 24 hours
  timezone: Asia/Tokyo
